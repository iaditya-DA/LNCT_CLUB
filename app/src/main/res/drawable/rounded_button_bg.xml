<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Button color -->
    <solid android:color="#E0E0E0" />

    <!-- Rounded corners -->
    <corners android:radius="12dp" />

    <!-- Optional: little padding inside shape -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
